# scoring-mcp — one MCP server per bounded context (§3.5).
# Tools = writes (score_click). Resources = reads (current_bounds, circuit_state).
[project]
name = "msm-scoring-mcp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
    "httpx>=0.27",
    "pydantic>=2.7",
]
